import Input from "../shared/Input";

const LoginForm = () => {
    return (
        <div>
            <Input inputType="email"/>
            <Input inputType="password"/>
        </div>
    )
}

export default LoginForm;